<?php
// Compatibility helpers placeholder.
// (Kept for consistency with other refactor packages.)

if (!function_exists('rf_compat_loaded')) {
    function rf_compat_loaded(): bool { return true; }
}
